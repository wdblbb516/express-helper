server:
  port: 8083

spring:
  application:
    name: station-service
  cloud:
    nacos:
      discovery:
        enabled: true
        server-addr: localhost:8848

logging:
  level:
    com.example.stationservice: INFO